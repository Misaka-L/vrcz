<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:VRCZ.Desktop.ViewModels"
             xmlns:models="clr-namespace:VRCZ.Core.Models;assembly=VRCZ.Core"
             xmlns:u="https://irihi.tech/ursa"
             xmlns:views="clr-namespace:VRCZ.Desktop.ViewModels.Views"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:DataType="views:ProfileSelectionViewModel"
             x:Class="VRCZ.Desktop.Views.ProfileSelectionView">

    <Interaction.Behaviors>
        <EventTriggerBehavior EventName="Loaded">
            <InvokeCommandAction Command="{Binding LoadProfilesCommand}" />
        </EventTriggerBehavior>
    </Interaction.Behaviors>

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" RowDefinitions="Auto,Auto">
        <ItemsControl ItemsSource="{Binding Profiles}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="viewModels:UserProfileItemViewModel">
                    <StackPanel>
                        <u:Avatar Classes="ExtraLarge" Source="{Binding ProfileImage^}"
                                  Command="{Binding LoadProfileCommand}"
                                  RenderOptions.BitmapInterpolationMode="HighQuality">
                            <u:Avatar.Styles>
                                <Style Selector="Image">
                                    <Setter Property="Stretch" Value="UniformToFill" />
                                </Style>
                            </u:Avatar.Styles>
                        </u:Avatar>
                        <TextBlock FontSize="16" Margin="0,16,0,0" HorizontalAlignment="Center" Text="{Binding UserProfile.DisplayName}" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <HyperlinkButton Grid.Row="1" Margin="0,16,0,0" HorizontalAlignment="Center" Content="New Profile"
                         Command="{Binding CreateProfileCommand}" />
    </Grid>
</UserControl>
